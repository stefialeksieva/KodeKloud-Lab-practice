#Create inventory file as per requirment with the following information
vim ~/playbook/inventory

[all]
stapp01 ansible_host=IP.IP.IP.IP ansible_connection=ssh ansible_user=tony ansible_password='XXXX' file_owner=tony
stapp02 ansible_host=IP.IP.IP.IP ansible_connection=ssh ansible_user=steve ansible_password='XXXX' file_owner=steve
stapp03 ansible_host=IP.IP.IP.IP ansible_connection=ssh ansible_user=banner ansible_password='XXXX' file_owner=banner


#Create yaml file with following content
vim playbook.yml 

---
- hosts: all
  become: true
  tasks:
    - name: Create /opt/app.txt with correct permissions
      file:
        path: /opt/app.txt
        state: touch
        mode: "0655"
        owner: "{{ file_owner }}"
        group: "{{ file_owner }}"
